//this just allows for instantiation of the observer class since the update method will be defined for those classes
//that derive from the interface
#include "Observer.h"

Observer::Observer()
{
  //default constructor, no members
}

Observer::~Observer()
{
  //destructor, no pointer members to delete
}

